# 对练·康复师管理端 — 本地数据测试前端 技术文档

> 版本：v1.0（待确认）
> 日期：2026-08-20

---

## 一、项目概述

| 项目 | 说明 |
|------|------|
| 项目名称 | 对练·康复师管理端（本地数据测试版） |
| 项目目标 | 1:1 复刻截图中的康复师管理端界面，使用本地 Mock 数据，用于前端功能调试与数据展示测试 |
| 运行方式 | 本地启动，浏览器访问，无需后端服务 |
| 数据来源 | 内置 JSON Mock 数据，可在页面中切换/编辑测试数据 |

---

## 二、技术栈选型

| 类别 | 选型 | 理由 |
|------|------|------|
| 构建工具 | **Vite** | 启动快、热更新即时，适合本地测试 |
| 框架 | **Vue 3（Composition API）** | 组件化清晰，与 Element Plus 生态契合 |
| UI 组件库 | **Element Plus** | 表单、表格、日期选择器、弹窗等组件齐全，蓝色主题可定制 |
| 图表库 | **ECharts 5** | 环形图、柱状图、折线曲线图（关节角度）均可覆盖 |
| 路由 | **Vue Router 4** | 顶部导航 + 子标签页切换 |
| 状态管理 | **Pinia**（可选，轻量场景可用 props/emit） | 患者选中状态、全局数据共享 |
| 样式 | **SCSS + CSS 变量** | 主题色统一管理 |
| Mock 数据 | 本地 `*.json` / `*.ts` 静态文件 | 无需后端，直接 import |

> **备选方案**：若希望零依赖、单文件即可打开，可改用纯 HTML + Tailwind CSS + ECharts CDN。是否采用备选请确认。

---

## 三、整体布局

```
┌─────────────────────────────────────────────────────────────┐
│  顶部导航栏（高 56px，蓝色 #0078C8 渐变）                      │
│  [Logo 对练·康复师管理端]  [患者管理] [动作库] [线下治疗] [方案库]  [用户头像] │
├──────────┬──────────────────────────────────────────────────┤
│          │  子标签栏（训练报告 / 康复计划 / 健康档案 / 步态分析）    │
│  左侧     ├──────────────────────────────────────────────────┤
│  患者列表  │                                                  │
│  (240px)  │              主内容区（自适应宽度）                   │
│          │                                                  │
│  - 科室名  │                                                  │
│  - 搜索框  │                                                  │
│  - 标签切换 │                                                  │
│  - 患者卡片 │                                                  │
└──────────┴──────────────────────────────────────────────────┘
```

- 左侧患者列表：**固定宽度 240px**，可折叠
- 主内容区：**最小宽度 1024px**，超出横向滚动
- 顶部导航：**固定高度 56px**

---

## 四、页面与路由设计

| 路由路径 | 页面 | 对应截图 |
|----------|------|----------|
| `/patient/report` | 患者管理 → 训练报告 | Snipaste_09-15-11 / 09-15-34 |
| `/patient/plan` | 患者管理 → 康复计划 | Snipaste_09-15-51 |
| `/patient/health` | 患者管理 → 健康档案 | Snipaste_09-16-07 / 09-17-25 |
| `/patient/gait` | 患者管理 → 步态分析 | Snipaste_09-16-19 ~ 09-17-08 |
| `/action-library` | 动作库 | Snipaste_09-17-40 / 09-17-54 |
| `/action-setting` | 动作及训练计划设置 | Snipaste_09-18-05 |
| `/offline` | 线下治疗 | 截图未覆盖，预留空页面 |
| `/plan-library` | 方案库 | 截图未覆盖，预留空页面 |

---

## 五、功能模块详细设计

### 5.1 左侧患者列表

- 科室标题：`儿童康复一科`
- 搜索框：输入后回车搜索
- 标签切换：`所有用户` / `分组` / `医生`
- 患者卡片：头像 + 姓名 + 性别年龄标签 + 消息图标 + 菜单图标
- 选中态：蓝色边框高亮
- 刷新按钮

### 5.2 训练报告页

**子标签**：病历1 / 训练报告1 / 训练报告2

| 区块 | 内容 | 图表类型 |
|------|------|----------|
| 总体完成率 | 已完成 / 未按时完成 / 待完成 | 环形图（ECharts doughnut） |
| 每周完成度·质量统计 | 康复完成度、康复达标率、健身操完成度、健身操达标率、总训练时间 | 分组柱状图 + 折线 |
| 疼痛评分与身体改善评分 | 日期维度评分趋势 | 折线图（预留） |
| 患者反馈 | 日期切换 + 训练动作难度评分（0-10） | 评分条 UI |
| 训练详细情况 | 第1次/第2次/第3次切换 + 日期导航 | 内容区（预留空态） |

### 5.3 康复计划页

**子标签**：居家训练 / 线下治疗

- 顶部参数：每天训练次数、疗程天数、起止日期
- 操作按钮：修改方案、添加新疗程
- 动作卡片列表：
  - 动作预览图 + 动作名称 + 左右标识（L / LR）
  - 休息时间（可选 60s/30s/15s）
  - 每组次数、组数、组间休息
- 动作示例：靠墙微蹲30秒、扶站踮脚、原地踏步练习（两个8拍）、扶手交替高抬腿

### 5.4 健康档案页

**个人健康信息表**（两栏布局）：

| 左栏 | 右栏 |
|------|------|
| 姓名*、性别*、出生年月、身高、体重 | 用药情况、是否影响睡眠、近4周发烧盗汗 |
| 手机号码*、所在门店、手术/受伤日期 | 其他不适、已采取措施（多选） |
| 诊断、诊治医院、主治医生、渠道 | 治疗期望（多选）、禁忌（多选） |
| 底部：取消 / 保存 | — |

**训练记录区**：
- 患者信息卡（姓名、性别、身高体重、术后/伤后）
- 时间筛选：全部 / 最近2周 / 最近1个月 / 最近3个月 / 自定义
- 日历卡片网格：日期 + 居家训练 + 状态（已完成/待完成）+ 查看详情
- 添加检查/评估报告按钮

### 5.5 步态分析页

**子标签**：步态视频 / 分析报告 / 步态对比

#### 步态视频
- 视频上传：左侧视频 / 右侧视频 / 正面视频
- 拍摄模式：定点拍摄 / 跟随拍摄
- 开始识别按钮 + 进度百分比
- 视频预览区 + 帧导航（第0帧，左右箭头）
- 关键帧标记：L-触地 / L-离地 等，可编辑帧号
- 线段实际长度设置（默认 200cm）
- 播放速度：1.0 / 1/2 / 1/4
- 棍图开关

#### 分析报告
- 患者基本信息：姓名、性别、年龄、身高、体重、测试时间、临床诊断
- **步态时空参数表**：步幅（L/R）、步幅对称性、步幅身高比、步宽、步频、步速、步态周期（L/R）、步态周期对称性、支撑相（L/R）、支撑相占比、支撑相对称性，含 3-5岁/6-12岁/13-18岁 参考范围列
- **关节角度曲线图**：髋关节（前屈/后伸）、膝关节（屈曲/过伸）、踝关节（背屈/跖屈），左右侧对比折线
- **关节角度范围表**：髋关节/膝关节/踝关节 × 运动方向 × L/R 数值 + 参考范围

### 5.6 动作库页

- 左侧筛选面板：
  - 身体部位：头颈、胸椎、腰腹核心、肩、肘、髋、膝、足踝、全身
  - 体位：站位、坐位、卧位、跪姿
  - 辅助器械：徒手、瑜伽垫、弹力带、哑铃、瑞士球、靠背椅、泡沫轴、沙袋、迷你弹力带
  - 儿童特殊训练
- 动作卡片网格：动作图 + 名称 + 播放按钮，点击选中（蓝色边框）
- 下方分类区：康复训练 / 健身功法·健身操（已选动作展示，可删除）
- 底部操作：下一步 / 保存 / 导入方案

### 5.7 动作及训练计划设置页

- 左侧：动作参数设置卡片
  - 左右标识切换（L/R）
  - 休息时间（60s/30s/15s 按钮）
  - 每组次数、组数、组间休息（数字加减）
  - 动作时长显示
- 中间：推送设置
  - 训练预计时长
  - 每天训练次数、疗程天数
  - 开始日期 / 结束日期（日历组件）
  - 是否需要患者上传视频（是/否）
- 右侧：选择患者推送
  - 搜索 + 所有用户/分组标签
  - 患者复选列表
  - 发送按钮

---

## 六、数据模型（Mock）

### 6.1 患者 Patient
```typescript
interface Patient {
  id: string;
  name: string;
  gender: '男' | '女';
  age: number;
  avatar: string;       // 头像颜色/占位
  department: string;   // 科室
  height?: number;
  weight?: number;
  phone?: string;
}
```

### 6.2 训练报告 TrainingReport
```typescript
interface TrainingReport {
  patientId: string;
  overallCompletion: { done: number; overdue: number; pending: number };
  weeklyStats: Array<{
    date: string;
    rehabCompletion: number;
    rehabStandard: number;
    fitnessCompletion: number;
    fitnessStandard: number;
    totalMinutes: number;
  }>;
  feedback: Array<{ date: string; difficulty: number }>;
}
```

### 6.3 康复计划 RehabPlan
```typescript
interface RehabPlan {
  patientId: string;
  dailySessions: number;
  courseDays: number;
  startDate: string;
  endDate: string;
  actions: Array<{
    id: string;
    name: string;
    image: string;
    side: 'L' | 'R' | 'LR';
    restTime: number;
    repsPerSet: number;
    sets: number;
    restBetweenSets: number;
  }>;
}
```

### 6.4 步态分析 GaitAnalysis
```typescript
interface GaitAnalysis {
  patientId: string;
  testTime: string;
  spatioTemporal: Array<{
    param: string;
    left?: string;
    right?: string;
    symmetry?: string;
    ref3_5: string;
    ref6_12: string;
    ref13_18: string;
  }>;
  jointAngleCurves: {
    hip: { left: number[]; right: number[] };
    knee: { left: number[]; right: number[] };
    ankle: { left: number[]; right: number[] };
  };
  jointRange: Array<{
    joint: string;
    direction: string;
    left: string;
    right: string;
  }>;
}
```

### 6.5 动作库 Action
```typescript
interface Action {
  id: string;
  name: string;
  image: string;
  category: '康复训练' | '健身功法/健身操';
  bodyPart: string[];
  position: string[];
  equipment: string[];
}
```

---

## 七、UI 设计规范

| 元素 | 规格 |
|------|------|
| 主色 | `#0078C8`（深蓝） / `#25B7C9`（青蓝，辅助/渐变） |
| 成功色 | `#36CFC9`（薄荷绿，已完成/保存按钮） |
| 警告色 | `#FAAD14`（橙，未按时完成） |
| 文字主色 | `#1F2937` |
| 文字次要色 | `#6B7280` |
| 边框色 | `#E5E7EB` |
| 背景色 | `#F5F7FA` |
| 卡片圆角 | `8px` |
| 主字体 | `PingFang SC / Microsoft YaHei / sans-serif` |
| 字号 | 标题 18-24px / 正文 14px / 辅助 12px |

---

## 八、项目目录结构

```
demo/
├── index.html
├── package.json
├── vite.config.ts
├── src/
│   ├── main.ts
│   ├── App.vue
│   ├── router/index.ts
│   ├── stores/              # Pinia（可选）
│   ├── layouts/
│   │   └── MainLayout.vue   # 顶部导航 + 左侧列表 + 主内容
│   ├── components/
│   │   ├── PatientSidebar.vue
│   │   ├── TopNavbar.vue
│   │   ├── ActionCard.vue
│   │   └── ...
│   ├── views/
│   │   ├── patient/
│   │   │   ├── TrainingReport.vue
│   │   │   ├── RehabPlan.vue
│   │   │   ├── HealthRecord.vue
│   │   │   └── GaitAnalysis.vue
│   │   ├── ActionLibrary.vue
│   │   └── ActionSetting.vue
│   ├── mock/
│   │   ├── patients.json
│   │   ├── reports.json
│   │   ├── plans.json
│   │   ├── gait.json
│   │   └── actions.json
│   └── styles/
│       └── variables.scss
└── public/
    └── images/              # 动作示例图等
```

---

## 九、开发计划（预估）

| 阶段 | 内容 | 预估 |
|------|------|------|
| 1 | 项目初始化 + 布局框架 + 路由 + Mock 数据骨架 | — |
| 2 | 患者列表 + 训练报告页（图表） | — |
| 3 | 康复计划页 + 健康档案页 | — |
| 4 | 步态分析页（视频区 + 参数表 + 角度曲线） | — |
| 5 | 动作库页 + 计划设置页 | — |
| 6 | 联调、细节打磨、本地运行验证 | — |

---

## 十、待确认事项

1. **技术栈**：Vue 3 + Element Plus + ECharts 方案是否 OK？还是希望纯 HTML 零依赖方案？
2. **覆盖范围**：是否所有截图页面都要做？线下治疗、方案库截图未覆盖，是否预留空页即可？
3. **数据**：Mock 数据用截图中的示例数据（艾承奕、7岁、步幅 107cm 等），还是需要可编辑/可切换多组测试数据？
4. **交互深度**：表单是否需要真实校验和保存（存 localStorage）？还是纯展示即可？
5. **步态视频**：是否需要真实视频播放，还是用静态图占位即可？
6. **动作图片**：截图中的动作示例图是否需要保留？还是用占位图？
